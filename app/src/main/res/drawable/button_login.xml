<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/magenta" />

    <gradient
        android:startColor="@color/pink_peach"
        android:centerColor="@color/magenta"
        android:endColor="@color/purple_clove"
        android:angle="135" />

    <!-- 边缘线条颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/magenta" />
    <corners
        android:topLeftRadius="@dimen/radius_button"
        android:topRightRadius="@dimen/radius_button"
        android:bottomLeftRadius="@dimen/radius_button"
        android:bottomRightRadius="@dimen/radius_button" />

</shape>